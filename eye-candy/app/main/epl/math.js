const { sqrt } = Math;
const { sin } = Math;
const { cos } = Math;
const { PI } = Math;
const { round } = Math;
const { floor } = Math;
const { ceil } = Math;

export { sqrt, sin, cos, PI, round, floor, ceil };
